services:
  - type: web
    name: finance-agent-grandmaster
    env: node
    buildCommand: bash build.sh
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: GROQ_API_KEY
        sync: false # Set this in the Render UI
